# ISOC（Isochronous Transfer）详解

## 一句话定义

**ISOC = USB 的实时传输模式。保证带宽、保证延迟，但不保证数据完整。** 视频和音频流专用。

---

## USB 四种传输类型对比

| 类型 | 带宽保证 | 延迟保证 | 错误重传 | 典型用途 |
|------|:--:|:--:|:--:|------|
| **Control** | 部分 | 无 | 有 | 设备枚举、配置 |
| **Bulk** | 无 | 无 | 有 | U 盘、打印机 |
| **Interrupt** | 有限 | 有 | 有 | 键盘、鼠标 |
| **ISOC** | ✅ 固定 | ✅ 固定 | ❌ 无 | 摄像头、麦克风 |

ISOC 的核心取舍：**宁丢数据，不丢时间**。视频丢一个包 = 画面花一瞬；延迟超过一帧 = 音画不同步。

---

## ISOC 如何工作

### USB Full-Speed (12 Mbps)

```
每 1ms 一帧
┌─────────────────────────┐
│ SOF │ ISOC │ Interrupt │ Bulk/Control │ EOF │
└─────────────────────────┘
      ↑
      每帧最多 1023 字节 ISOC 数据
```

### USB High-Speed (480 Mbps) — ESP32-P4 使用此模式

```
每 125μs 一个微帧（8 微帧 = 1ms）
┌──────────────────────────────┐
│ μSOF │ ISOC │ ... │ μEOF    │
└──────────────────────────────┘
        ↑
        每微帧最多 1024 字节 × 3 事务 = 3072 字节
```

### 关键参数

```
端点描述符定义了 ISOC 端点能力：
  wMaxPacketSize:
    bit 0-10: 最大包大小 (MPS)，HS 最大 1024 字节
    bit 11-12: 额外事务数 (MULT)，可多 0-2 个事务/微帧
               即 MULT=0 → 1 事务, MULT=2 → 3 事务

  计算: 每微帧带宽 = MPS × (MULT + 1)
        例: 1024 × (2 + 1) = 3072 字节/微帧
        每秒带宽 = 3072 × 8000 = ~24 MB/s
```

---

## ISOC 在 UVC 摄像头中的角色

```
摄像头 sensor → JPEG 编码 → ISOC 发送 → USB 主机 ISOC 接收 → JPEG 解码 → 显示
                              ↑                                  ↑
                         无 CRC 校验                      包可能丢失/跳过
                         无 ACK 应答                      数据可能损坏
                         无重传机制
```

### ISOC 包结构（UVC Payload Header）

每个 ISOC 微帧传输的包都有 UVC 定义的头部：

```
┌────────────────┬──────────────────────────────────┐
│ bHeaderLength  │ 头部长度 (2-12 字节)              │
├────────────────┼──────────────────────────────────┤
│ bmHeaderInfo   │ bit0: FID (帧 ID, 0/1 交替)     │
│                │ bit1: EOF (帧结束标志)            │
│                │ bit2: PTS (表示有时戳)            │
│                │ bit6: EOH (头部结束)              │
│                │ bit7: ERR (摄像头报错!)           │
├────────────────┼──────────────────────────────────┤
│ payload data   │ JPEG 比特流片段                   │
└────────────────┴──────────────────────────────────┘
```

### 一帧 JPEG 的 ISOC 传输过程

```
摄像头 JPEG 编码完一帧 (50KB)
  → 拆成多个 ISOC 包
  → 每个微帧发送一个包 (最多 3072 字节)
  → 50KB ÷ 3KB ≈ 17 个微帧 ≈ 2.1ms 发送完

接收端拼接：
  微帧 1: FID=0, EOF=0 → "新帧开始"
  微帧 2-16: FID=0, EOF=0 → "同一帧的数据"
  微帧 17: FID=0, EOF=1 → "本帧结束"
```

---

## ESP32-P4 DWC2 ISOC 的固有问题

### 问题 1: 调度深度有限

DWC2 控制器的 Periodic Schedule 只有约 **16-32 个 slot**。PC 的 xHCI 有 128+ 个。这意味着：

- 如果有太多 ISOC URB 同时排队 → 硬件调度表溢出 → 部分微帧被 SKIPPED
- SKIPPED ≠ ERROR — 驱动视为"无害"放行，但 JPEG 比特流缺字节 → 解码失败

### 问题 2: Multi-Transaction 未启用（已在 v2.4.2 修复）

ESP32-P4 `usb_host_uvc` v2.4.2 之前的版本只用了 1 个事务/微帧（MPS=1024），而不是 USB HS 允取的最大 3 个事务（3×1024=3072）。导致实际带宽只有期望的 1/3。

### 问题 3: PSRAM 缓存一致性问题

ESP32-P4 的 CPU 通过 cache 访问 PSRAM，而 USB DMA 和 JPEG 解码器直接访问 PSRAM。如果 cache 没有正确同步（writeback/invalidate），就会出现：

- **解码前未 writeback** → DMA 读到旧数据 → 解码结果是上一帧的一部分 → 重影
- **解码后未 invalidate** → CPU 读到 cache 中的旧数据 → 显示上一帧 → 重影

这就是为什么我们的代码里有三步 `esp_cache_msync()`。

---

## 为什么 UVC + ISOC 在 PC 上完美、在 ESP32-P4 上有问题

| 对比维度 | PC (xHCI) | ESP32-P4 (DWC2) |
|----------|-----------|-----------------|
| ISOC 调度深度 | 128+ | 16-32 |
| 微帧管理 | 硬件环形缓冲 | 软件维护 linked list |
| DMA 引擎 | 专用，高带宽 | 共享 AXI 总线 |
| 错误恢复 | 硬件重试 | 无，丢包即丢数据 |
| 驱动成熟度 | 20 年 | 2 年 |
| Cache 管理 | x86 自动 MESI | 需要手动 msync |

---

## 本项目的 ISOC 配置

```c
// camera.c — uvc_host_stream_config_t
.advanced = {
    .number_of_urbs = 4,           // 4 个 URB
    .urb_size = 16 * 1024,         // 每个 16KB
    .number_of_frame_buffers = 3,  // 3 个帧缓冲
    .frame_size = 200 * 1024,      // 每帧最大 200KB (MJPG 实际 ~50KB)
};
```

### URB 工作流程

```
初始: 提交 4 个 URB（每个 16KB，约 5 个 ISOC 包）
  │
  ├─ URB 1 完成 → ISOC 回调处理 → 重新提交 URB 1
  ├─ URB 2 完成 → ISOC 回调处理 → 重新提交 URB 2
  ├─ URB 3 完成 → ISOC 回调处理 → 重新提交 URB 3
  └─ URB 4 完成 → ISOC 回调处理 → 重新提交 URB 4
  
4 个 URB × 16KB = 64KB in-flight 缓冲 = ~3 个 MJPG 帧的安全余量
```

---

## ISOC vs BULK — 为什么摄像头用 ISOC 而不是 BULK

| | ISOC | BULK |
|------|------|------|
| 带宽 | 保证 (~24MB/s HS) | 剩余带宽（可能为 0） |
| 延迟 | 保证（每 125μs） | 不保证 |
| 可靠性 | 不保证 | 保证（CRC + 重传） |
| 适用 | 实时流 | 文件传输 |

摄像头选 ISOC 因为：
- 视频帧有严格时序要求（33ms@30fps）
- 丢几个包 → 花几行像素，人眼不敏感
- BULK 在 USB 总线忙时可能被无限推迟 → 视频卡死

---

## 总结

ISOC 是 USB 为实时数据设计的"不完美但准时"的传输模式。ESP32-P4 的 DWC2 主机控制器实现有限，调度深度低、multi-transaction 长期未启用、需要手动 cache 管理。配合适当的 URB 配置、乒乓缓冲和缓存同步，MJPG @30fps 是可行的。
